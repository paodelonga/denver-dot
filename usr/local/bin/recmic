#!/usr/bin/bash

test -d "$HOME/Music/Audiorecords/" || mkdir -p "$HOME/Music/Audiorecords/"
ffmpeg -f alsa -ac 2 -i default -preset ultrafast $(test -n "$1" && echo "$1" || date "+$HOME/Music/Audiorecords//audio_%Y.%m.%d-%H_%M_%S.mp3")
